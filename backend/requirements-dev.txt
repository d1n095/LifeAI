# Test-only dependencies — never installed in the production image (see backend/Dockerfile).
-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
# Real binary the Development Operator's run_profile("ruff_check") shells out to
# (app/development_operator/service.py) — must be on PATH for tests that exercise that
# governed capability for real, not just import it.
ruff==0.15.8
