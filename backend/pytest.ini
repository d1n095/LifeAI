[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    backend: general backend unit/integration tests (no browser)
    security: RLS and session-security tests
    account: account-lifecycle and rate-limit tests
